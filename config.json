{
  "context": {
    "check_enabled": true,
    "_comment": "check_enabled: set false to skip context prompts before critical operations"
  },
  "vertex_ai": {
    "project": null,
    "location": null,
    "_comment": "Required for Vertex AI: set project and location. Falls back to gcloud config (project) and GOOGLE_CLOUD_LOCATION env var."
  },
  "external_review": {
    "alert_if_missing": true,
    "feedback_iterations": 1,
    "_comment_alert": "alert_if_missing: set false to skip external LLM checks silently",
    "_comment_iterations": "feedback_iterations: number of review/integrate cycles (0 = skip external review entirely). More than 1 feedback iteration is not yet implemented"
  },
  "models": {
    "gemini": "gemini-2.5-pro",
    "chatgpt": "gpt-4.1",
    "_comment": "Models for external plan review. Alternatives \u2014 Gemini: gemini-3.1-pro-preview, gemini-2.5-flash, gemini-3-flash-preview | OpenAI: gpt-5.4-mini, gpt-5.4, o4-mini, gpt-4o"
  },
  "llm_client": {
    "timeout_seconds": 120,
    "max_retries": 3,
    "retry_codes": [
      429,
      500,
      503
    ],
    "_comment": "Timeout and retry settings for external LLM calls"
  },
  "goalloop": {
    "max_iterations": 0,
    "question_budget": 2,
    "floor_bits": 0.15,
    "_comment_max": "max_iterations: iteration ceiling for /deep goalloop. 0 = only the goal, a blocker, or the user stops the loop. --max-iters overrides.",
    "_comment_questions": "question_budget / floor_bits: defaults for pick-questions.py inside goalloop. An unattended run should ask rarely; interactive plan and discovery may use the CLI defaults (4 / 0.15)."
  }
}
